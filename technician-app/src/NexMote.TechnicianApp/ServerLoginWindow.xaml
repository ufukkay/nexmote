<Window x:Class="NexMote.TechnicianApp.ServerLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NexMote - Sunucu Girişi" Height="360" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#090D16">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="NexMote Technician Console" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,4" />
        <TextBlock Grid.Row="1" Text="Sunucu adresi ve teknisyen erişim anahtarını girin." Foreground="#94A3B8" FontSize="12" Margin="0,0,0,16" TextWrapping="Wrap" />

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="Sunucu Adresi" Foreground="#94A3B8" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
            <TextBox x:Name="ServerUrlBox" Background="#0F172A" Foreground="White" BorderBrush="#334155" BorderThickness="1" Padding="8,6" />
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <TextBlock Text="Teknisyen Erişim Anahtarı" Foreground="#94A3B8" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
            <PasswordBox x:Name="TechnicianKeyBox" Background="#0F172A" Foreground="White" BorderBrush="#334155" BorderThickness="1" Padding="8,6" />
            <TextBlock Text="Sunucudaki nexmote-first-run-credentials.txt dosyasından ya da Sunucu Ayarları sayfasından alın." Foreground="#64748B" FontSize="10.5" Margin="0,4,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <TextBlock x:Name="ErrorText" Grid.Row="4" Foreground="#F87171" FontSize="11" TextWrapping="Wrap" VerticalAlignment="Top" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="İptal" Width="90" Height="32" Margin="0,0,8,0" Click="Cancel_Click" Background="#1E293B" Foreground="White" BorderThickness="0" />
            <Button Content="Bağlan" Width="90" Height="32" Click="Connect_Click" Background="#2563EB" Foreground="White" BorderThickness="0" />
        </StackPanel>
    </Grid>
</Window>
